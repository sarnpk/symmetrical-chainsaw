[build]
  command = "cd reclaim-app && npm install && npm run build"
  publish = "reclaim-app/.next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "18"
  NPM_FLAGS = "--legacy-peer-deps"

# Edge Functions configuration
# Note: Edge functions are defined individually with [[edge_functions]]
# The directory is set in the plugin configuration

# Build optimization
[build.processing]
  skip_processing = false

[build.processing.css]
  bundle = true
  minify = true

[build.processing.js]
  bundle = true
  minify = true

[build.processing.html]
  pretty_urls = true
